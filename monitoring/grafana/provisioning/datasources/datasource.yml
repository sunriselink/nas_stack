apiVersion: 1

datasources:
  - name: InfluxDB v1 (InfluxQL)
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    user: ${INFLUXDB_USERNAME}
    isDefault: true
    jsonData:
      dbName: ${INFLUXDB_DB}
      httpMode: GET
    secureJsonData:
      password: ${INFLUXDB_PASSWORD}
